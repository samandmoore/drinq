name: drinq
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3
  freezed_annotation: any
  state_notifier: any
  flutter_riverpod: 0.6.0-dev+2
  hooks_riverpod: 0.6.0-dev+2
  flutter_hooks: any
  uuid: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  freezed: any
  build_runner: any

flutter:
  uses-material-design: true
